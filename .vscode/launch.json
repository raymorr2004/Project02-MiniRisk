{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Mini-RISK",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main",
      "cwd": "${workspaceFolder}",
      "args": [],
      "preLaunchTask": "Build Risk (all sources)",
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb"
    }
  ]
}
